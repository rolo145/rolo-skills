# Runtime output — regenerated per run, never committed.
_artifacts/

# _feedback/ is deliberately NOT ignored: it is the skill's improvement
# history and should travel with the skill when shared.

.DS_Store
